#include "main.h"
/**
* _abs - compute the absolute value of a number
* @r: The integer to find the absolute value of.
* return: return r or -r
*
*/
int _abs(int r)
{

if (r < 0)
return (-r);
else
return (r);

return (0);
}
